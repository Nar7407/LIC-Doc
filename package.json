{
  "name": "lic-doc",
  "private": true,
  "version": "1.0.0",
  "description": "Insurance advisory customer database",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend run test"
  }
}
